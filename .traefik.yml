displayName: "FritzBox Local-Redirect"
type: middleware
icon: icon.png
import: github.com/MrEAlderson/FritzBox-LocalRedirect
summary: "Fallback to local hostname in case client visits from a local network"

testData:
  Headers:
    X-Demo: test
    X-URL: '{{URL}}'